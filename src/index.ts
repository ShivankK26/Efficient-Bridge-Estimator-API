/**
 * @file index.ts
 * @description Entry point for bootstrapping the application.
 * Imports the main application module to initialize necessary configurations and start the server.
 */

import './app';